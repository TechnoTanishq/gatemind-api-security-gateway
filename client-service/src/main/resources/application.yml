server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gatemind_client
    username: tanishq
    password: tanishq123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info